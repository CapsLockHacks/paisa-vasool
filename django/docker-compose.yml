version: '3'

services:
  redis:
    image: redis:5-alpine
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    env_file:
      - app.env
    ports:
      - "8000:8000"
    depends_on:
      - redis